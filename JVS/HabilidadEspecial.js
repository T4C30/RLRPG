import Clase from "./Clase";

class HabilidadEspecial {
    realizarHabilida(clase){
        if (clase.getNombre() == "Picaro") {
            
        }

        if (clase.getNombre() == "Guerrero") {
            
        }

        if (clase.getNombre() == "MAgo") {
            
        }
    }
}